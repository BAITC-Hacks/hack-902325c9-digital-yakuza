# prior/ — что говорит история смен тарифов

Скрипт строит таблицы и вшивает их в `agent.py` (агент во время работы файлы не читает):

```bash
cd beeline_agent
python prior/build_prior.py          # чистка + оценка + вшивка PRIOR, PRIOR_UNSEEN, PRIOR_SCORE в agent.py
python prior/evaluate_estimators.py  # сравнение способов оценки (split-half), ~10 с
python prior/similarity.py           # сходство истории и аудитории, ~5 с
python prior/verify_prior.py         # независимая перепроверка всех чисел приора, ~5 с
```

| Словарь в `agent.py` | Что внутри |
|---|---|
| `PRIOR_FORMAT = ("q", "q_se", "n_obs")` | формат кортежей; проверка R6 тренажёра сверяет его с агентом |
| `PRIOR[(from, seg, to)] = (q, q_se, n_obs)` | 451 тройка с историей; по умолчанию `PRIOR_MODE = "raw"` — q ровно по формуле среды (функция организаторов `_mock_impact_model`, совпадение до 1e−16; п. 5, п. 7) |
| `PRIOR_UNSEEN[(from, seg, to)] = (q, q_se, 0)` | 809 троек ячеек аудитории без истории: 720 — 12 тарифов без истории, 89 — пробелы по ячейкам |
| `PRIOR_PARTS[(from, seg, to)] = (pct, conv)` | из чего сложен q (все 1 260 троек): чтобы разброс переноса можно было масштабировать долей сменивших (sd ∝ conv) — редкий переход не может дать большой эффект |
| `PRIOR_SCORE[(from, seg, to)] = (q_level, q_contrast, q_se)` | разложение q для всех 1 260 троек (см. п. 2) |

Единицы (договорённость с агентом):
- `q` — база эффекта: Δ% × доля перехода, до множителя канала, доля от ARPU абонента; эффект на канале = `q × m_c`;
- `q_se` — стандартная ошибка оценки q по истории, в единицах q; размер выборки уже учтён — агент берёт как есть,
  на √n не делит. Перенос «история → аудитория» — отдельная настройка агента `TRANSFER_SD`:
  `sd = √(q_se² + TRANSFER_SD²)`;
- `n_obs` — число наблюдений, только для справки.

Как считается `q_se` (дельта-метод для q = pct × conv):
- `PRIOR`, режим raw: `q_se = √((conv·se_pct)² + (pct·conv_se)²)`, `se_pct = pct_std/√n`, где разброс Δ% подтянут
  к разбросу сегмента (иначе при n = 2–3 случайно близкие значения дают ложную уверенность),
  `conv_se = √(conv(1−conv)/N_ячейки)`;
- режим hier: то же, но `se_pct` = апостериорная sd из иерархии (размер выборки в ней уже учтён),
  `conv_se = √(conv(1−conv)/(N + α + 1))`;
- `PRIOR_UNSEEN`: `q_se = √((conv·pct_unc)² + (pct·cv·conv)²)`, где
  `pct_unc = √(pct_sd² + (pct − pct_fallback)²)` — иерархия (в ней уже ошибка переноса контраста, LOO ≈ 0.24)
  плюс расхождение с fallback-правилом среды (0.4 × Δцены / медиана цены), а `cv ≈ 0.93` — коэффициент вариации
  доли перехода между тройками с историей (для тройки без данных долю мы по сути не знаем).

Проверка по корзинам n_obs (медиана `q_se / |q|`): 1–2 набл. — 1.46, 3–5 — 1.10, 6–20 — 0.70, 21–50 — 0.48,
50+ — 0.34; у `PRIOR_UNSEEN` — 1.98 (медиана `q_se` 0.041 против 0.029 у `PRIOR`). В абсолютных единицах
у редких переходов `q_se` мала потому, что мала доля перехода: редкий переход не может дать большой эффект.

Пилот на канале c покажет примерно `m_c·q` плюс шум `0.804/√n`. Это стартовая оценка и ранжирование, а не деньги.
Файлы: `prior_table.csv`, `prior_unseen.csv` (+ соответствие пакета), `breakeven.csv` (пороги каналов),
`reports/` (сравнение оценок, сходство, параметры).

### Словарь обозначений

Короткие имена в коде и что они значат. Ключ всех таблиц — (текущий тариф, сегмент ARPU, целевой тариф) = `(from, seg, to)`.

| Имя | Где | Что это |
|---|---|---|
| `pct`, Δ% | история | относительное изменение ARPU после смены тарифа: (после − до) / до, обрезано в [−1; 3], как в среде |
| `conv`, доля сменивших | история | какая доля ушедших с тарифа `from` (в сегменте `seg`) выбрала именно `to`; в среде — вероятность согласия на предложение |
| `q` | `PRIOR` | ожидаемый прирост ARPU на один контакт, `pct × conv`, до множителя канала; в деньгах: `q × m_c × ARPU абонента` |
| `q_se` | `PRIOR` | стандартная ошибка `q`: насколько оценка может ошибаться из-за малой выборки |
| `n_obs` | `PRIOR` | сколько смен тарифа в истории стоит за оценкой (0 — истории нет) |
| `m_c` | среда | множитель отклика канала: push 0.50, sms 0.65, digital_ads 0.85, call 1.20 |
| `level`, `q_level` | `hier.py` | уровень страты `(from, seg)`: общий сдвиг ARPU у всех, кто ушёл с этого тарифа; в основном регрессия к среднему |
| `contrast`, `q_contrast` | `hier.py` | насколько эта цель лучше или хуже средней цели страты — надёжная часть сигнала |
| `target_effect`, `seg_target_effect`, `cell_effect` | `hier.py` | три слоя контраста: цель → сегмент × цель → ячейка |
| `tau` (τ) | `hier.py` | разброс настоящих эффектов между группами; чем он меньше, тем сильнее малые группы подтягиваются к общему среднему |
| `pct_std` | `prior_table.csv` | разброс Δ% внутри тройки; у малых групп подтянут к разбросу сегмента |
| `pct_unc` | `prior_unseen.csv` | полная неопределённость Δ% тройки без истории: модель + расхождение с правилом среды |
| `fit` | `prior_unseen.csv` | насколько пакет целевого тарифа покрывает потребление ячейки (0–1) |
| `TRANSFER_SD` | `agent.py` | насколько эффект на нашей аудитории может отличаться от истории (в единицах q); неопределённость агента `sd = √(q_se² + TRANSFER_SD²)` |
| `PRIOR_SHRINK` | `agent.py` | доля доверия истории: стартовая оценка агента = `0.5 × q` |
| `mu`, `sd` | `agent.py` | текущая оценка эффекта кампании и её неопределённость; после каждого пилота уточняются |
| `lcb`, `RISK_K` | `agent.py` | пессимистичная оценка `mu − RISK_K·sd`; кампания идёт в план, только если она > 0 |
| `ucb` | `agent.py` | оптимистичная оценка `mu + sd`; по ней агент выбирает, что проверить пилотом |
| `0.804` | среда | шум пилота на одного абонента; ошибка пилота из n абонентов — `0.804/√n` |
| `strength`, `contrast_rho` | `sim/worlds.py` | насколько мир отличается от истории: сила сдвига уровней и доля контраста истории, которая переносится |
| потолок, `upper_bound` | `eval/core.py` | сколько максимум можно заработать, зная истинные эффекты |
| T4 | `eval/checks.py` | «только история», без пилотов, в долях потолка; по ТЗ без разведки результат ≈ в 15 раз меньше |

## 0. Проверка исследования команды

| Утверждение | Проверка | Итог |
|---|---|---|
| История — одни и те же ~14.8 тыс. абонентов, с аудиторией не пересекается | ID: 14 817 / 14 991 / 14 875, во всех трёх 14 727; с аудиторией — 0 | да |
| `AVG_ARPU_PREV_3M` = среднее `arpu_monthly` за июль–сентябрь | совпадает у 100% (среднее по строкам как есть, вместе с конфликтующими дублями) | да |
| Все смены — октябрь 2026 | да | да |
| Плацебо: без смен LOW «растёт» +2.45, HIGH «падает» −0.21 (лог-шкала) | воспроизведено при log(x+1): LOW +2.45, MID −0.28, HIGH −0.21. В Δ% среды: LOW +0.88, MID +0.23, HIGH −0.04 (у реальных смен +1.02 / +0.26 / −0.13) | да — уровень сегментов в основном регрессия к среднему |
| Контрасты внутри страты надёжны: 0.96 / 0.90 / 0.71 | split-half + Спирмен–Браун: цель 0.96, сегмент×цель 0.92, ячейка 0.71 (в Δ%; в лог-шкале ячейка 0.56) | да |
| Целей в истории 9 из 21; 64% релевантных троек без наблюдений, 9% с 30+ | 64% и 8% | да |
| Adversarial AUC ≈ 0.71 | 0.69 (только ARPU) — 0.73 (все признаки из профиля) | да |
| HEAVY 50% против 29%, HIGH 59% против 30% | 50% / 29%; HIGH 59% / 30% (35% после чистки) | да |
| Отношение ARPU после/до: у 5% ≤ 0, у 5% ≥ 10× | ≤ 0 у 7.5%, ≥ 10× у 4.7% | почти (≤ 0 чаще) |
| Шум пилота из ТЗ: сигнал/шум на абонента ≈ 0.123, 100 абонентов → 11% ошибок знака, 90% → 108, 95% → 178 | Φ(−0.123·√30) = 0.25, Φ(−0.124·√200) = 0.04; остальное сходится | да |
| Перенос на 12 тарифов по сходству атрибутов | leave-one-out на 9 известных целях: RMSE 0.25 против 0.24 у «нуля» (без tariff_1 — 0.16 против 0.13) | **нет**: цена и пакет контраст не предсказывают, поэтому контраст новых тарифов = 0 с широкой неопределённостью |
| Переплата сверх пакета ведёт к смене на больший пакет | с перерасходом трафика на больший пакет данных уходят 75% (без него 44%); по минутам 53% против 48% | да для трафика, слабо для минут; такие переходы дают на 0.10 меньший Δ% (переплата исчезает) |
| Лог-отношение + винзоризация как цель | среда считает среднее клипнутого Δ%; в Δ% контрасты ячеек надёжнее (0.71 против 0.56) | оставлен клипнутый Δ% |
| Запуск по 20-му квантилю постериора: тот же средний результат, P(минус) 9% → 1.5% | агент Димаша на 60 мирах `random` (`python -m sim.hypotheses --worlds 60 --grid "RISK_K=0,0.84,1.0"`): RISK_K 0 → 0.84 — средний 1.02 → 1.09 млн, 10-й перцентиль −130 тыс. → −14 тыс., убыточных кампаний 81 → 42, но P(итог < 0) 15% → 15% | направление подтверждается (хвост и убыточные кампании), P(минус) не падает: минусы дают сами пилоты |

## 1. Чистка `data/change_tariff.csv` (`prior/history.py`)

| Что | Строк | Решение | Почему |
|---|---:|---|---|
| Полные дубли | 6 | убраны из разбросов и иерархии; в сыром среднем (режим raw) оставлены | одна смена записана дважды, но среда считает эффект по всем строкам файла — с дублями; без них q у 51 тройки отличался от формулы среды (до 0.033) |
| ARPU до смены = 0 | 1 406 | убраны | Δ% не определён |
| ARPU до смены < 0 | 1 | убрана | то же; это корректировка начислений |
| ARPU до смены в (0; 100) | 711 | убраны | Δ% взрывается; среда (мок) считает с того же порога 100 |
| Δ% > +300% | 843 | обрезаны до +300% | правило среды |
| ARPU после = 0 (−100%) | 962 | оставлены | реальный исход — это и есть downsell |
| ARPU до смены > p99.9 (до 333 тыс.) | 15 | оставлены | Δ% относительный и снизу ограничен −100% |

В расчёт идут 12 699 из 14 823 строк. Порог 100 проверен на чувствительность: средний Δ% в LOW 1.17 (порог 1) →
1.02 (100) → 0.90 (500).

## 2. Модель: уровень страты + контраст цели (`prior/hier.py`)

Плацебо показывает: общий сдвиг ARPU внутри страты «тариф_откуда × сегмент» — это в основном регрессия
к среднему, одинаковая для любых целей. Поэтому
`Δ%(from, seg, to) = L(from, seg) + C(from, seg, to)`:
- **L — уровень страты**: сжат к среднему сегмента (LOW +1.02, MID +0.26, HIGH −0.13). Внутри истории
  стабилен (split-half 0.87), но это дрейф, и в реальной аудитории он может быть другим.
- **C — контраст цели**: иерархический EB «цель → сегмент×цель → ячейка», сила сжатия на каждом уровне —
  методом моментов (τ = 0.18 / 0.13 / 0.14). Контраст по целям: tariff_9 +0.23, 8 +0.09, 11 +0.08,
  10 +0.07, 12 +0.01, 4 −0.03, 13 −0.14, 21 −0.18, 1 −0.61.
- Для 12 тарифов без истории C = 0 («как средняя цель страты»), sd ≈ 0.30: ранжировать их по истории нельзя,
  только пилотом.

Split-half (30 разбиений, цель — то, что считает среда), RMSE Δ%:

| Оценка | 1–5 набл. | 6–20 | 21+ | все | ранговая корр. |
|---|---:|---:|---:|---:|---:|
| **уровень + контраст (hier)** | **0.58** | 0.42 | **0.14** | **0.28** | **0.77** |
| сжатие к ценовой регрессии (прошлая версия) | 0.61 | 0.42 | 0.15 | 0.29 | 0.75 |
| простое среднее (мок) | 0.78 | 0.47 | 0.16 | 0.33 | 0.71 |
| медиана | 0.95 | 0.71 | 0.26 | 0.48 | — |

`PRIOR_SCORE` отдаёт обе части в единицах q: `q_level = L·доля`, `q_contrast = C·доля`, `q_se` — ошибка их суммы. Для «структурного»
постериора агента: `эффект ≈ a + b1·q_level + b2·q_contrast + отклонение руки`. Если b1 ≈ 0 — дрейф истории
в этой среде не переносится; b2 — насколько переносится ранжирование целей (оно надёжнее).

Доля перехода: у троек с историей — сглаженная к структуре переходов сегмента (α = 10); у троек без истории —
медианная (так делает fallback среды) × (0.5 + 0.5·fit), где fit — доля абонентов ячейки, чей трафик и минуты
помещаются в пакет тарифа (`prior/unseen.py`, метки «подходит / частично / не подходит» в `prior_unseen.csv`).

## 3. Пороги каналов (`breakeven.csv`)

Для каждой ячейки аудитории по среднему predicted_arpu (A):
- `be_<канал>` — канал окупается сам: `q > cost / (m·A)`; для tariff_8/HIGH звонок — 1.7%, SMS — 0.08%;
- `be_<канал>_vs_<дешевле>` — канал лучше предыдущего с учётом цены бюджета (1 у.е. в финале ≈ 10 у.е. прироста):
  `q > Δcost·10 / (Δm·A)`; для tariff_8/HIGH звонок против рекламы — 50%, реклама против SMS — 11.5%,
  SMS против push — 3.4%.

## 4. Похожа ли история на аудиторию (`reports/similarity.json`)

Средний ARPU 6 413 против 3 647; HIGH 59% против 30%; HEAVY 50% против 29%; медиана трафика 2 029 против
232 МБ; overlap структуры тарифов 0.55; adversarial AUC 0.69–0.73. Веса важности из adversarial-модели
оставляют ~54% эффективной выборки. В приор по умолчанию не включены: главная неопределённость — дрейф
уровня, а его перевзвешивание по признакам не исправит; это закрывают пилоты и `q_level` в постериоре.

## 5. A/B на мирах: что приор даёт агенту

Точность на истории ≠ результат агента. Текущий агент Димаша, одни и те же миры, меняется только `PRIOR`:

| Проверка | Сырой q (как мок) | Сжатие к цене (v1) | Уровень + контраст |
|---|---:|---:|---:|
| 20 «честных» миров: приор по половине A истории, истина — по непересекающейся половине B (`prior/ab_worlds.py`) | 4.21 млн (медиана) | — | 3.99 млн; разница −0.18 ± 0.23 млн, лучше в 30% миров |
| 60 миров `random` (sim) — средний / P(итог < 0) | 1.04 млн / 13% | 0.90 млн / 20% | 0.88 млн / 13% |
| 20 миров `resample` (sim; смещено в пользу сырого q — та же история) | 4.12 млн | 3.73 млн | 3.67 млн |

Разница в пределах шума, но направление везде одно: с текущей логикой агента (кандидат = q > 0, разведка по
оптимистичной оценке) сжатая оценка не помогает — у неё больше положительных q (304 против 263, в основном
малые ячейки LOW/MID, подтянутые дрейфом уровня), и 20 пилотов распыляются. Поэтому в `PRIOR` по умолчанию
оставлен сырой q (`PRIOR_MODE = "raw"`, мок снова даёт 4 377 794), а иерархическая оценка идёт в
`PRIOR_SCORE` и колонки `q_hier`, `q_level`, `q_contrast` — для структурного постериора. Переключение:
`PRIOR_MODE = "hier"` в `build_prior.py`. Решать — прогоном на мирах после изменений агента.

## 6. Проверка утверждений о среде (по коду `environment.py` / `scoring_core.py`)

| Утверждение | Итог |
|---|---|
| Эффект = pct × conv × множитель канала, зависит только от (текущий тариф, ARPU-сегмент, целевой тариф, канал) | да для относительного эффекта; точнее `pct × min(conv × m, 1)` — у звонка упирается в 1 при conv > 0.83. Эффект в деньгах ещё × predicted_arpu каждого абонента. Абоненты без тарифа или сегмента (95 + 5) идут через fallback-правило |
| Фильтры data/call эффект не меняют, только выбирают, кого включить; можно делить ячейку между каналами | да; но выигрыш скромный: внутри ячейки predicted_arpu по call_segment отличается на ±10% (HIGH +9%, LOW −7%) |
| Канал только умножает эффект — SMS-пилот измеряет тот же базовый эффект | да (с той же оговоркой про `min(…, 1)`) |
| Шум пилота известен точно: 0.804/√n | да для пилота по одной ячейке. Если в фильтре несколько тарифов, к шуму добавляется разброс состава выборки: пилот усредняет эффекты разных ячеек без веса по ARPU. n — фактический из ответа `run_pilot` (может быть меньше запрошенного) |
| Пилотные абоненты идут в зачёт; повторный контакт оплачивается, эффект — один раз | да; дедупликация берёт **лучший** эффект абонента, поэтому финальная кампания может «перекрыть» неудачный пилот на тех же абонентах (за доплату) |
| Лимиты применяются по порядку списка — ценные кампании первыми | да; плюс обрезка внутри кампании идёт по возрастанию ID_NUMBER, а не по ценности: кампанию, которая упрётся в лимит, лучше заранее сузить фильтрами |
| Итоговый расчёт детерминирован, случайность только в пилотах | да: финальный скоринг без шума; случайны выборка абонентов пилота и шум его среднего |

## 7. Перепроверка (`prior/verify_prior.py`)

```bash
python prior/verify_prior.py   # код выхода 1, если что-то не сошлось
```

Независимый пересчёт, без кода `build_prior.py` и `hier.py`:

- 9 счётчиков чистки пересчитаны с нуля;
- `q` в `PRIOR` совпадает с формулой среды (`_mock_impact_model`) для всех 451 тройки;
- `q_se` сходится с формулами выше;
- все 1 260 троек аудитории (63 ячейки × цели) есть ровно в одной таблице;
- таблицы, вшитые в `agent.py`, совпадают с CSV. Если нет, выдаётся предупреждение: значит, `build_prior.py` не перезапускали.

Что нашла перепроверка и что исправлено:

1. 6 полных дублей раньше убирались и из сырого среднего. Из-за этого `q` у 51 тройки отличался от формулы среды, максимум на 0.033 (tariff_15 / LOW → tariff_9). Теперь в режиме raw среднее и доля считаются ровно функцией среды, а дубли убираются только из разбросов и иерархии.
2. Счётчик «Δ% = −100%» на самом деле считал Δ% ≤ −99.9%: это 1 064 строки, из них ровно −100% у 962. Счётчик переименован в `pct_at_most_-99.9%_kept`.

Имена в `hier.py` и `sim/worlds.py` заменены на понятные (`target_effect` вместо `ct` и т. п.). Результат не изменился: CSV совпадают побайтно, 72 мира `make_world` (12 сценариев × 6 seed) совпадают по хэшам, и порядок вызовов на мир не влияет.
